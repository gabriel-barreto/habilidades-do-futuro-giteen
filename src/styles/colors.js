import { css } from 'styled-components';

const colors = css`
  :root {
    --primaryColor: #ffc813;
    --secondaryColor: #ff9600;
    --auxColor: #2ecc71;

    --dark: #2b2b2b;
    --darkest: #000;

    --light: #ededed;
    --lightest: #fff;
  }
`;

export default colors;
