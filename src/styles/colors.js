import { css } from 'styled-components';

const colors = css`
  :root {
    --primaryColor: #f1d302;
    --secondaryColor: #e0271b;
    --auxColor: #20bf55;

    --dark: #212121;
    --darkest: #000;

    --light: #ededed;
    --lightest: #fff;
  }
`;

export default colors;
